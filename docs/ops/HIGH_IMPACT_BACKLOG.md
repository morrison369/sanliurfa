# High Impact Backlog

## Yüksek etki / düşük maliyet

1. Kalan wrapper yüzeylerini `LoadingWrapper` standardına taşı
2. `copy:visible:audit` kurallarına yasaklı İngilizce UI terimleri ekle
3. Kritik admin ve public yüzeylere Playwright contract assertion ekle
4. `api-envelope` kullanımını kalan helper modüllerde tamamla
5. Search sonuçları için boş durum CTA ekle

## Yüksek etki / yüksek maliyet

1. `src/lib` domain bazlı refactor
2. `messaging-inbox` davranış turu
3. `subscription-manager` ve `billing-history` görev odaklı redesign
4. `vendor-dashboard` ürünleştirme
5. Admin dashboard’larda source/scope/updated standardı

## Bir haftalık sıra

1. wrapper guard + kalan wrapper migrasyonu
2. domain refactor hazırlığı
3. search ve notifications UX turu
4. subscription/billing akışı
5. vendor/business panel iyileştirmesi
