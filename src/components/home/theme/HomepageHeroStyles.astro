<style is:inline>
 .home-hero {
  position: relative;
  overflow: clip;
  background:
   radial-gradient(circle at top left, rgba(44, 122, 82, 0.1), transparent 30%),
   radial-gradient(circle at top right, rgba(212, 151, 24, 0.15), transparent 34%),
   radial-gradient(circle at 75% 20%, rgba(184, 101, 43, 0.08), transparent 28%),
   linear-gradient(180deg, var(--landing-sand) 0%, var(--landing-cream) 48%, #f8f3eb 100%);
  padding: clamp(5.75rem, 8vw, 7rem) 0 clamp(2rem, 5vw, 3.5rem);
 }

 .home-hero::before {
  content: '';
  position: absolute;
  inset: 0;
  pointer-events: none;
  background:
   linear-gradient(180deg, rgba(255, 255, 255, 0.26) 0%, rgba(255, 255, 255, 0) 28%),
   radial-gradient(circle at center, transparent 48%, rgba(31, 20, 16, 0.08) 100%);
 }

 .home-hero-content {
  position: relative;
  z-index: 1;
 }

 .hero-shell {
  padding-top: 0;
  padding-bottom: 0;
 }

 .hero-stage {
  display: grid;
  gap: 1.5rem;
  align-items: start;
 }

 .hero-copy {
  max-width: 42rem;
 }

 .hero-eyebrow {
  display: inline-flex;
  align-items: center;
  gap: 0.55rem;
  margin-bottom: 1rem;
  padding: 0.45rem 1rem;
  border-radius: 999px;
  border: 1px solid rgba(31, 95, 85, 0.2);
  background: rgba(255, 255, 255, 0.74);
  box-shadow: 0 14px 26px -22px rgba(31, 20, 16, 0.45);
  color: var(--landing-green);
  font-family: 'Jost', system-ui, sans-serif;
  font-size: 0.72rem;
  font-weight: 800;
  letter-spacing: 0.18em;
  text-transform: uppercase;
 }

 .hero-eyebrow::before {
  content: '';
  width: 0.55rem;
  height: 0.55rem;
  border-radius: 999px;
  background: var(--landing-gold);
  box-shadow: 0 0 0.75rem rgba(212, 151, 24, 0.55);
 }

 .hero-title {
  margin: 0;
  max-width: 12ch;
  font-family: 'Cormorant Garamond', Georgia, serif;
  font-size: clamp(2.7rem, 9vw, 5.35rem);
  font-style: italic;
  font-weight: 700;
  line-height: 0.98;
  letter-spacing: -0.03em;
  background: linear-gradient(135deg, #1c2018 0%, #31584f 48%, #b8652b 100%);
  -webkit-background-clip: text;
  background-clip: text;
  -webkit-text-fill-color: transparent;
 }

 .hero-title-main,
 .hero-title-accent {
  display: block;
 }

 .hero-title-accent {
  margin-top: 0.6rem;
  max-width: 18ch;
  font-family: 'Jost', system-ui, sans-serif;
  font-size: clamp(1rem, 3vw, 1.4rem);
  font-style: normal;
  font-weight: 700;
  line-height: 1.3;
  color: var(--landing-copper);
  -webkit-text-fill-color: initial;
 }

 .hero-sub {
  margin: 1rem 0 0;
  max-width: 38rem;
  color: #4d4a43;
  font-size: 1rem;
  line-height: 1.8;
 }

 .hero-copy :global(.ssb) {
  max-width: 42rem;
  margin-top: 1.25rem;
 }

 .hero-cta-row {
  display: flex;
  flex-wrap: wrap;
  gap: 0.75rem;
  margin-top: 1rem;
 }

 .hero-cta-primary,
 .hero-cta-secondary {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 3.25rem;
  padding: 0 1.35rem;
  border-radius: 999px;
  font-family: 'Jost', system-ui, sans-serif;
  font-size: 0.82rem;
  font-weight: 800;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  text-decoration: none;
  transition: transform 0.18s ease, box-shadow 0.18s ease, border-color 0.18s ease;
 }

 .hero-cta-primary {
  background: linear-gradient(135deg, var(--landing-copper) 0%, var(--landing-gold) 100%);
  color: #fff;
  box-shadow: 0 18px 28px -18px rgba(184, 101, 43, 0.55);
 }

 .hero-cta-secondary {
  border: 1px solid rgba(31, 95, 85, 0.14);
  background: rgba(255, 255, 255, 0.82);
  color: var(--landing-green);
 }

 .hero-cta-primary:hover,
 .hero-cta-secondary:hover {
  transform: translateY(-2px);
 }

 .hero-inline-pills {
  display: flex;
  flex-wrap: wrap;
  gap: 0.65rem;
  margin-top: 1rem;
 }

 .hero-inline-pill {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 2.6rem;
  padding: 0 1rem;
  border-radius: 999px;
  border: 1px solid rgba(24, 35, 29, 0.08);
  background: rgba(255, 255, 255, 0.7);
  color: #30463f;
  text-decoration: none;
  font-size: 0.82rem;
  font-weight: 600;
  transition: transform 0.18s ease, border-color 0.18s ease, box-shadow 0.18s ease;
 }

 .hero-inline-pill:hover {
  transform: translateY(-2px);
  border-color: rgba(49, 88, 79, 0.3);
  box-shadow: 0 16px 24px -20px rgba(31, 20, 16, 0.5);
 }

 .hero-console {
  position: relative;
 }

 .hero-console-panel {
  position: relative;
  overflow: hidden;
  padding: 1.35rem;
  border-radius: 1.6rem;
  border: 1px solid rgba(24, 35, 29, 0.08);
  background: linear-gradient(180deg, rgba(255, 255, 255, 0.9) 0%, rgba(255, 255, 255, 0.76) 100%);
  box-shadow: 0 28px 48px -34px rgba(24, 35, 29, 0.45);
  backdrop-filter: blur(16px);
 }

 .hero-console-panel::before {
  content: '';
  position: absolute;
  inset: auto -12% 70% auto;
  width: 14rem;
  height: 14rem;
  border-radius: 999px;
  background: radial-gradient(circle, rgba(212, 151, 24, 0.2) 0%, rgba(212, 151, 24, 0) 72%);
  pointer-events: none;
 }

 .hero-console-top {
  display: flex;
  flex-direction: column;
  gap: 0.4rem;
  margin-bottom: 1rem;
 }

 .hero-console-kicker {
  margin: 0;
  color: var(--landing-copper);
  font-size: 0.72rem;
  font-weight: 800;
  letter-spacing: 0.18em;
  text-transform: uppercase;
 }

 .hero-console-title {
  margin: 0;
  color: #1e211a;
  font-family: 'Cormorant Garamond', Georgia, serif;
  font-size: clamp(1.7rem, 4vw, 2.3rem);
  line-height: 1;
 }

 .hero-console-sub {
  margin: 0;
  color: #5b564d;
  font-size: 1rem;
  line-height: 1.7;
 }

 .hero-console-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 0.85rem;
  margin-bottom: 1rem;
 }

 .hero-console-card {
  display: flex;
  flex-direction: column;
  gap: 0.35rem;
  min-height: 8.5rem;
  padding: 1rem;
  border-radius: 1.1rem;
  border: 1px solid rgba(24, 35, 29, 0.08);
  background: rgba(248, 243, 235, 0.82);
  color: inherit;
  text-decoration: none;
  transition: transform 0.18s ease, border-color 0.18s ease, box-shadow 0.18s ease;
 }

 .hero-console-card:hover {
  transform: translateY(-3px);
  border-color: rgba(49, 88, 79, 0.26);
  box-shadow: 0 18px 24px -22px rgba(24, 35, 29, 0.38);
 }

 .hero-console-icon {
  width: 2.75rem;
  height: 2.75rem;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  border-radius: 1rem;
  background: linear-gradient(135deg, rgba(184, 101, 43, 0.12), rgba(31, 95, 85, 0.1));
  color: var(--landing-green);
 }

 .hero-console-icon :global(svg) {
  width: 1.2rem;
  height: 1.2rem;
 }

 .hero-console-metric {
  color: #1e211a;
  font-family: 'Cormorant Garamond', Georgia, serif;
  font-size: 1.75rem;
  font-weight: 700;
  line-height: 1;
 }

 .hero-console-label {
  color: var(--landing-green);
  font-size: 0.76rem;
  font-weight: 800;
  letter-spacing: 0.12em;
  text-transform: uppercase;
 }

 .hero-console-desc {
  margin-top: auto;
  color: #61594f;
  font-size: 0.8rem;
  line-height: 1.55;
 }

 .hero-console-links {
  display: grid;
  gap: 0.75rem;
 }

 .hero-console-links-head {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  gap: 1rem;
 }

 .hero-console-links-title {
  margin: 0;
  color: var(--landing-green);
  font-size: 0.76rem;
  font-weight: 800;
  letter-spacing: 0.16em;
  text-transform: uppercase;
 }

 .hero-console-links-copy {
  margin: 0;
  color: #786f64;
  font-size: 0.76rem;
 }

 .hero-console-chip-row {
  display: flex;
  flex-wrap: wrap;
  gap: 0.55rem;
 }

 .hero-console-chip {
  display: inline-flex;
  align-items: center;
  min-height: 2.35rem;
  padding: 0 0.9rem;
  border-radius: 999px;
  border: 1px solid rgba(24, 35, 29, 0.08);
  background: rgba(255, 255, 255, 0.78);
  color: #2c3c35;
  text-decoration: none;
  font-size: 0.8rem;
  font-weight: 600;
 }

 .hero-console-chip:hover {
  border-color: rgba(184, 101, 43, 0.32);
 }

 .stat-bar {
  position: relative;
  z-index: 1;
  margin-top: 1.5rem;
  border-top: 1px solid rgba(24, 35, 29, 0.08);
 }

 .stat-bar-inner {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 0.85rem;
  padding: 1rem 0 0;
 }

 .stat-item {
  display: flex;
  flex-direction: column;
  gap: 0.18rem;
  min-height: 5.25rem;
  padding: 1rem 1rem 0.9rem;
  border-radius: 1.1rem;
  border: 1px solid rgba(24, 35, 29, 0.08);
  background: rgba(255, 255, 255, 0.72);
  box-shadow: 0 16px 30px -26px rgba(24, 35, 29, 0.36);
 }

 .stat-num {
  display: inline-block;
  color: var(--landing-copper);
  font-family: 'Cormorant Garamond', Georgia, serif;
  font-size: 1.55rem;
  font-weight: 700;
  line-height: 1;
 }

 .stat-lbl {
  color: #575046;
  font-size: 0.75rem;
  font-weight: 700;
  letter-spacing: 0.08em;
  text-transform: uppercase;
 }

 .stat-sep {
  display: none;
 }

 @media (max-width: 767px) {
  .hero-cta-row,
  .hero-inline-pills {
   display: grid;
   grid-template-columns: 1fr;
  }

  .hero-cta-primary,
  .hero-cta-secondary,
  .hero-inline-pill {
   width: 100%;
  }

  .hero-console-links-head {
   flex-direction: column;
   align-items: flex-start;
   gap: 0.35rem;
  }
 }

 @media (max-width: 639px) {
  .home-hero {
   padding-top: 5.4rem;
  }

  .hero-title {
   max-width: none;
  }

  .hero-title-accent,
  .hero-sub {
   max-width: none;
  }

  .hero-console-grid,
  .stat-bar-inner {
   grid-template-columns: 1fr;
  }
 }

 @media (min-width: 1024px) {
  .hero-stage {
   grid-template-columns: minmax(0, 1.12fr) minmax(22rem, 0.88fr);
   gap: 2rem;
  }
 }

 @media (min-width: 768px) {
  .stat-bar-inner {
   grid-template-columns: repeat(3, minmax(0, 1fr));
  }
 }

 @media (min-width: 1200px) {
  .stat-bar-inner {
   grid-template-columns: repeat(6, minmax(0, 1fr));
  }
 }

 @media (prefers-reduced-motion: reduce) {
  .hero-cta-primary,
  .hero-cta-secondary,
  .hero-inline-pill,
  .hero-console-card,
  .hero-console-chip {
   transition: none;
  }
 }
</style>
