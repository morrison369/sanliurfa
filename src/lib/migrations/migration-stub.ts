import { logger } from '../logging';
/**
 * Migration Stub
 * Placeholder for missing migration imports
 */

export const migrationStub = {
  up: async () => logger.info('Migration stub: up'),
  down: async () => logger.info('Migration stub: down')
};

export default migrationStub;
