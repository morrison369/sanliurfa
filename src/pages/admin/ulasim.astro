---
import Layout from '../../layouts/Layout.astro';
import BusRouteManager from '../../components/admin/BusRouteManager';

if (!Astro.locals.isAdmin && Astro.locals.user?.role !== 'admin') {
  return Astro.redirect('/giris?redirect=/admin/ulasim');
}

const seo = {
  title: 'Otobüs Hat Yönetimi - Admin',
  description: 'Şanlıurfa otobüs hatları ve sefer saatleri yönetimi',
  noIndex: true,
};
---

<Layout seo={seo}>
  <div class="bg-gray-50 min-h-screen pt-24 pb-10">
    <div class="container mx-auto px-4 max-w-5xl">
      <div class="mb-6">
        <a href="/admin" class="text-sm text-gray-500 hover:text-gray-700">← Admin Paneli</a>
      </div>
      <BusRouteManager client:idle />
    </div>
  </div>
</Layout>
