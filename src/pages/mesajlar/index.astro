---
import Layout from '../../layouts/Layout.astro';
import MessagingInbox from '../../components/MessagingInbox.astro';

// Redirect if not logged in
if (!Astro.locals.user) {
  return Astro.redirect('/giris');
}
---

<Layout title="Mesajlar - Şanlıurfa Rehberi">
  <div class="min-h-screen bg-gray-50">
    <MessagingInbox currentUserId={Astro.locals.user.id} />
  </div>
</Layout>

<style>
  div :global(body) {
    overflow: hidden;
  }
</style>
