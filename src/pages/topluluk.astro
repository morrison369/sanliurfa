---

import Layout from '../layouts/Layout.astro';
import { SocialFeatures } from '../components/SocialFeatures';
import { requireAuth } from '../lib/auth';
import { getSiteBranding } from '../lib/site-branding';
const { baseUrl } = await getSiteBranding();

const auth = await requireAuth(Astro.request);
const isAuthenticated = Boolean(auth.user);

const seo = {
  title: 'Şanlıurfa Topluluk Özellikleri | Takip, Mesajlaşma ve Trendler',
  description:
    'Şanlıurfa topluluk sayfasında aktivite akışı, trend etiketler, takipçi yönetimi, mesajlaşma ve eşleşme özellikleri.',
  keywords: ['şanlıurfa topluluk', 'urfa sosyal ağ', 'şanlıurfa üyeler', 'urfa yerel sosyal', 'şanlıurfa takip et'],
  ogImage: '/images/hero/hero-home.webp',
  canonical: '/topluluk',
};

const cards = [
  {
    title: 'Takipçi Ağı',
    desc: 'Kullanıcıları takip edin, profil etkileşimini artırın.',
    href: isAuthenticated ? '/takipciler' : '/kayit?redirect=/takipciler',
  },
  {
    title: 'Mesajlaşma',
    desc: 'Topluluk üyeleriyle doğrudan mesajlaşma deneyimi.',
    href: isAuthenticated ? '/mesajlar' : '/kayit?redirect=/mesajlar',
  },
  {
    title: 'Eşleşme',
    desc: '4 fotoğraflı profil ile sağa/sola kaydırarak üyelerle eşleşin.',
    href: '/eslesme',
  },
  {
    title: 'Üyelik ve Planlar',
    desc: 'İlk aşama ücretsiz erişim ve gelecek premium plan detaylarını inceleyin.',
    href: '/fiyatlandirma',
  },
];

const faqItems = [
  {
    S: 'Şanlıurfa topluluk özellikleri ücretsiz mi?',
    C: 'İlk aşamada takip, mesajlaşma, mekan ekleme, yorum ve eşleşme özellikleri ücretsiz olarak açık tutulur.',
  },
  {
    S: 'Üyeler birbirine mesaj atabilir mi?',
    C: 'Evet. Kayıtlı üyeler mesajlaşma akışı üzerinden birbirleriyle doğrudan iletişim kurabilir.',
  },
  {
    S: 'Eşleşme özelliğinde kaç fotoğraf kullanılabilir?',
    C: 'Eşleşme profilinde 4 fotoğrafa kadar görsel kullanılabilir ve üyeler sağa/sola kaydırma deneyimiyle eşleşebilir.',
  },
];
---

<Layout seo={seo}>
  <div class="min-h-screen bg-[#FDFAF3]">
    <section class="bg-[#0D0A08] border-b border-[rgba(200,160,100,0.14)] text-white py-16">
      <div class="container mx-auto px-4 max-w-5xl">
        <p class="text-sm font-semibold uppercase tracking-widest text-[#9A8470] mb-2">Şanlıurfa Platformu</p>
        <h1 class="text-4xl md:text-5xl font-bold text-[#F5EDD6] mb-4">Topluluk Özellikleri</h1>
        <p class="text-[#C4B49A] text-lg max-w-3xl">
          Şanlıurfa kullanıcı topluluğu için takip, trend ve etkileşim alanlarını tek sayfada yönetin.
        </p>
      </div>
    </section>

    <section class="py-10">
      <div class="container mx-auto px-4 max-w-5xl">
        <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-4 mb-8">
          {cards.map((card) => (
            <a
              href={card.href}
              class="rounded-sm border border-[rgba(200,160,100,0.18)] bg-white p-5 shadow-sm transition hover:-translate-y-0.5 hover:shadow-md hover:border-urfa-300"
            >
              <h2 class="text-lg font-bold text-[#1A1008]">{card.title}</h2>
              <p class="mt-1 text-sm text-[#6B5540]">{card.desc}</p>
            </a>
          ))}
        </div>

        {isAuthenticated ? (
          <div class="rounded-sm border border-[rgba(200,160,100,0.18)] bg-white p-6 shadow-sm">
            <h2 class="text-2xl font-bold text-[#1A1008] mb-2">Aktivite, Trend ve Takip</h2>
            <p class="text-sm text-[#6B5540] mb-6">
              Aşağıdaki modülde topluluk hareketlerini ve popüler etiketleri takip edebilirsiniz.
            </p>
            <SocialFeatures client:visible={{ rootMargin: '200px' }} />
          </div>
        ) : (
          <div class="rounded-sm border border-[rgba(200,160,100,0.18)] bg-white p-7 shadow-sm">
            <p class="text-sm font-semibold uppercase tracking-widest text-[#9A8470]">Ücretsiz topluluk erişimi</p>
            <h2 class="mt-2 text-3xl font-bold text-[#1A1008]">Takip, mesajlaşma ve eşleşme için ücretsiz hesap açın</h2>
            <p class="mt-3 max-w-2xl text-[#6B5540]">
              Topluluk özellikleri ilk aşamada ücretsizdir. Hesap açtıktan sonra kullanıcıları takip edebilir,
              mesajlaşabilir, mekan ekleyebilir, yorum yapabilir ve eşleşme modülünü kullanabilirsiniz.
            </p>
            <div class="mt-5 flex flex-wrap gap-3">
              <a href="/kayit?redirect=/topluluk" class="rounded-sm bg-urfa-600 px-5 py-3 font-semibold text-white hover:bg-urfa-700 transition-colors">
                Ücretsiz Hesap Aç
              </a>
              <a href="/giris?redirect=/topluluk" class="rounded-sm border border-[rgba(200,160,100,0.3)] px-5 py-3 font-semibold text-[#1A1008] hover:bg-[rgba(200,160,100,0.06)] transition-colors">
                Giriş Yap
              </a>
              <a href="/eslesme" class="rounded-sm border border-[rgba(200,160,100,0.2)] px-5 py-3 font-semibold text-urfa-700 hover:bg-[rgba(200,160,100,0.06)] transition-colors">
                Eşleşme Modülünü Gör
              </a>
            </div>
          </div>
        )}

        <section class="mt-8 rounded-sm border border-[rgba(200,160,100,0.18)] bg-white p-6 shadow-sm">
          <p class="text-sm font-semibold uppercase tracking-widest text-[#9A8470]">Hızlı Cevap</p>
          <h2 class="mt-1 text-2xl font-bold text-[#1A1008]">Şanlıurfa topluluk platformunda neler yapılır?</h2>
          <p class="mt-2 max-w-3xl text-sm leading-6 text-[#6B5540]">
            Üyeler birbirini takip edebilir, arkadaş olarak ekleyebilir, mesaj atabilir, mekan ekleyebilir,
            mekanlara puan ve yorum bırakabilir. Eşleşme modülünde 4 fotoğraflı profil ile sağa/sola kaydırma deneyimi sunulur.
          </p>
        </section>
      </div>
    </section>
  </div>
  <script type="application/ld+json" set:html={JSON.stringify({
    '@context': 'https://schema.org',
    '@type': 'FAQPage',
    mainEntity: faqItems.map((item) => ({
      '@type': 'Question',
      name: item.S,
      acceptedAnswer: {
        '@type': 'Answer',
        text: item.C,
      },
    })),
  })} is:inline />
  <script type="application/ld+json" set:html={JSON.stringify({
    '@context': 'https://schema.org',
    '@type': 'BreadcrumbList',
    itemListElement: [
      { '@type': 'ListItem', position: 1, name: 'Anasayfa', item: `${baseUrl}/` },
      { '@type': 'ListItem', position: 2, name: 'Topluluk', item: `${baseUrl}/topluluk` },
    ],
  })} is:inline />
</Layout>
